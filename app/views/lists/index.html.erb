<div class = container>
<h1>List movies</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
   <%= link_to "add a movie", new_list_path %>



</ul>
</div>
